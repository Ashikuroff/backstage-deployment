apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
  labels:
    app.kubernetes.io/name: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: backstage
  template:
    metadata:
      labels:
        app.kubernetes.io/name: backstage
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: backstage
          image: ghcr.io/ashikuroff/backstage:sha-3ed75cf39944ec855301cd289db767a785cf6959
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7007
              name: http
          env:
            - name: APP_BASE_URL
              value: http://localhost:7007
            - name: NODE_ENV
              value: production
            - name: AUTH_GITHUB_CLIENT_ID
              value: "Ov23liaCqvhltEGsOcev"
            - name: AUTH_GITHUB_CLIENT_SECRET
              value: "bc4a6caba75fc08be856bcb8e1ad3bcea01c552a"
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
          volumeMounts:
            - name: backstage-data
              mountPath: /var/backstage
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: backstage-data
          persistentVolumeClaim:
            claimName: backstage-data
        - name: tmp
          emptyDir: {}
